# Bug Audit — 2026-04-22

**Category:** Tools & Tech
**Created:** 2026-04-22
**Updated:** 2026-04-22

Глибокий аудит всього стеку: Python backend, FastAPI, React miniapp.
Знайдено 33 проблеми. Всі Critical та пріоритетні High зафіксовані в цю ж сесію.

---

## Critical (4) — всі виправлені

### 1. Race condition: double bankroll update
**Файли:** `scheduler/tasks/live_tracker.py`, `scheduler/tasks/update_results.py`

`live_tracker` (кожні 3 хв) і `update_results` (кожні 30 хв) одночасно завантажували одні й ті ж predictions з `result=NULL`, обидва рахували PnL і обидва викликали `_update_bankrolls` → bankroll збільшувався вдвічі.

**Фікс:**
- `update_results` тепер викликає `db.refresh(pred)` перед обробкою кожного prediction і пропускає ті, де `result is not None`
- Обидва таски передають у `_update_bankrolls` тільки список `newly_settled` — predictions що щойно змінились з None → win/loss
- `_update_bankrolls` підписаний `(db, newly_settled: list)` в обох файлах

### 2. N+1 queries в generate_picks_monster.py
**Файл:** `scheduler/tasks/generate_picks_monster.py`

`_get_odds(match.id, db)` викликався для кожного матчу в циклі — N запитів до БД замість одного.

**Фікс:**
- Видалено `_get_odds()`; додано `_load_odds_batch(match_ids, db)` — один `WHERE match_id IN (...)` перед циклом
- Аналогічно existing predictions — теж завантажуються одним запитом до циклу, замість 2×N запитів per-match

### 3. MODEL_VERSION = "v1" в generate_picks.py
**Файл:** `scheduler/tasks/generate_picks.py`

Стара ML v1 модель використовувала `MODEL_VERSION = "v1"`, але API очікує `"ws_gap_v1"`, `"monster_v1_kelly"` тощо. Picks від цього таска були б невидимі для фронтенду. **Але таск закоментований в scheduler/main.py** (`# ML v1 — вимкнено`), тому це мертвий код, не live-баг.

### 4. DB session leaks (audit flag)
Після перевірки: `try/finally: db.close()` присутній у всіх тасках. Менш критично ніж звітував аудит. Залишено як є.

---

## High (6) — всі виправлені

### 5. Bot handler без try/except
**Файл:** `bot/handlers.py`

`cmd_start` → `message.answer(...)` без захисту. При Telegram rate limit або network error бот крашився.

**Фікс:** Додано `try/except Exception` з `logger.warning(...)`.

### 6. PickCard: NaN при stake=null
**Файл:** `capper-miniapp/src/components/PickCard.tsx`

`const profit = (pick.stake * (pick.odds - 1)).toFixed(0)` — якщо `stake=null` (historical imports), вираз повертав `NaN` і рендерився в UI.

**Фікс:** `pick.stake != null ? ... : null`; всі money-row значення показують `—` при null.

### 7. Stale fetch race condition у PicksScreen
**Файл:** `capper-miniapp/src/screens/PicksScreen.tsx`

При швидкому перемиканні днів/моделей старий fetch міг зарезолвитись пізніше нового і перезаписати правильні дані. useEffect не мав cleanup.

**Фікс:** Додано `cancelled` flag + `return () => { cancelled = true }` в обидва useEffect (picks + stats). Якщо компонент/ефект перезапустився — stale відповідь ігнорується.

### 8. Відсутні LEAGUE_FLAGS для Netherlands / Belgium / Ukraine
**Файли:** `api/utils.py`, `api/main.py`

Monster model покриває Eredivisie, Jupiler Pro League, УПЛ — але всі три повертали `🏟` замість правильного прапора.

Додатково: УПЛ в БД зберігається як "Premier League" (колізія з England).

**Фікс:**
- `api/utils.py` LEAGUE_FLAGS: додано `"Eredivisie": "🇳🇱"`, `"Jupiler Pro League": "🇧🇪"`, `"Ukrainian Premier League": "🇺🇦"`, `"Premier League (UA)": "🇺🇦"`
- `api/main.py` `prediction_to_pick`: після базового lookup перевіряємо `league.country == "Ukraine"` → перевизначаємо flag на `"🇺🇦"`

---

## Medium (12) — виправлено 2026-04-22

| # | Файл | Проблема | Статус |
|---|------|----------|--------|
| 9 | `api/main.py:49–54` | `match.league.name`, `match.home_team.name` без null check | ✅ виправлено разом з UPL collision fix |
| 10 | `scheduler/main.py` | `run_early_picks_scan` (09:05) overlap з ws_gap (:05) | ✅ перенесено на 09:10 |
| 11 | `live_tracker.py` + `update_results.py` | Bankroll snapshot edge case при одночасному commit | залишено (мінімальний ризик, db.refresh захищає) |
| 12 | `confirm_picks.py:61–76` | Odds matching без `is_closing=False` фільтра | ✅ додано `Odds.is_closing.is_(False)` |
| 13 | `db/models.py` | Немає індексу на `(match_id, model_version, is_active)` | ✅ міграція g2b3c4d5e6f7 |
| 14 | `db/models.py:171–175` | Денормалізовані поля не синхронізуються при UPDATE | залишено (поля оновлюються тільки при import) |
| 15 | `CompareScreen.tsx` | SVG curve без `memo` | ✅ `React.memo` на MiniCurve і CombinedCurves |
| 16 | `PicksScreen.tsx` | `daily` object без `useMemo` | ✅ `useMemo([picks])` |
| 17 | `live_tracker.py:112` | Exception лог без типу помилки | ✅ `[type(e).__name__]` у лозі |
| 18 | `HistorySheet.tsx:189` | `pick.id` як key не унікальний між моделями | ✅ compound key `day-id` |
| 19 | `db/models.py:143–145` | Odds unique constraint race при паралельному fetch | залишено (рідкісний edge case) |
| 20 | `update_clv.py:27–55` | Exception handling нестабільний | ✅ per-match try/except |

Також виправлено поза аудитом:
- `timing` badge: `confirm_picks` тепер кожні 2 год замість 1×/день (09:10 UTC); API визначає timing в реальному часі

---

## Model-Level Bugs (знайдено 2026-04-22, виправлено 2026-04-22)

Глибокий ресерч трьох ML-моделей — знайдено і виправлено 5 баг-рівнів.

| # | Файл | Проблема | Статус |
|---|------|----------|--------|
| 21 | `generate_picks_ws_gap.py:549` | Early scan: `stake_kelly` без `KELLY_CAP` → необмежений розмір ставки | ✅ виправлено: `min(..., bankroll_kelly * KELLY_CAP)` |
| 22 | `generate_picks_ws_gap.py` | `ODDS_MAX` відсутній — аутсайдери з нестабільною ймовірністю проходили фільтр | ✅ виправлено: `ODDS_MAX = 4.0` + фільтр у `_ws_gap_pick` |
| 23 | `generate_picks_ws_gap.py:317,541` | ELO momentum завжди 0: `build_match_features` викликався без `elo_snapshots` → 2 фічі моделі мертві | ✅ виправлено: імпорт `build_elo_snapshots`; будується після `matches_df`; передається в обидва виклики |
| 24 | `model/monster/features.py:103-109,213-214` | XG false NaN: `0.0 or np.nan = np.nan` — команди з xGF=0.0 трактувались як missing data | ✅ виправлено: `x if x is not None else np.nan`; `isnan(x or np.nan)` → `isnan(x)` |
| 25 | `scheduler/tasks/update_monster_p_is.py:113-114` | Той самий XG false NaN у p_is recalculation | ✅ виправлено: `isnan(x)` замість `isnan(x or np.nan)` |

### Ключові висновки ресерчу
- `stake_kelly` і `stake_cap` — два окремі code path; cap в одному не захищає інший
- `x or np.nan` в Python: `0.0 or np.nan = np.nan` — небезпечний паттерн для числових фічей; завжди використовувати explicit `None` check
- ELO momentum (`home_elo_momentum`, `away_elo_momentum`) — ніколи не рахувався в продакшні через відсутність `elo_snapshots` param

---

---

## Deep Audit — Pass 2 (знайдено 2026-04-22, виправлено 2026-04-22)

Другий глибокий аудит всього стеку після першого раунду. Знайдено 19 нових багів.

| # | Файл | Проблема | Статус |
|---|------|----------|--------|
| 26 | `api/main.py:135` | `match.date.date() == date.today()` — UTC vs local date; timing badge неправильний для UTC± | ✅ `datetime.now(timezone.utc).date()` |
| 27 | `confirm_picks.py:29` | `today = date.today()` — те саме; timing early/final плутається для граничних годин | ✅ `datetime.now(timezone.utc).date()` |
| 28 | `update_clv.py:22` | `Match.date >= str(yesterday)` — рядкове порівняння datetime; матчі могли губитись | ✅ `datetime` об'єкти з `timezone.utc` |
| 29 | `generate_picks_monster.py:319` | Async `_send()` звертався до `match.home_team.name` (lazy load) після потенційного detach сесії | ✅ pre-extract `home_name`/`away_name`/`time_str` до `asyncio.run` |
| 30 | `generate_picks_monster.py:32` | `_P_IS_CACHE` без TTL → після щотижневого `update_monster_p_is` модель тиждень використовує стейлі p_is | ✅ TTL 24год; `(value, timestamp)` туплі |
| 31 | `CompareScreen.tsx:63` | `CombinedCurves`: `i / (len - 1)` → ділення на 0 при 1 ставці | ✅ `Math.max(len - 1, 1)` |
| 32 | `PicksScreen.tsx:11` | `const TODAY = new Date()` на рівні модуля — стейла після опівночі | ✅ `useMemo(() => new Date(), [])` всередині компонента |
| 33 | `PicksScreen.tsx:13` | `isoDate` використовував `toISOString()` (UTC) → неправильна дата для UTC± юзерів | ✅ `getFullYear/getMonth/getDate` (local) |
| 34 | `api/main.py:/api/history` | `days: int` без обмеження → `days=999999` могло покласти сервер | ✅ `ge=1, le=365` + `.limit(2000)` |
| 35 | `model/features/elo.py:84` | `compute_elo_momentum` повертав `0.0` при < N матчів → "нова команда" = "нульова динаміка" | ✅ `return np.nan` |
| 36 | `live_tracker.py`, `update_results.py`, `update_clv.py` | `except` блоки без `db.rollback()` → відкрита транзакція при помилці | ✅ `except: db.rollback(); raise` |
| 37 | `collect_data.py:58` | `db.commit()` раз на день — при збої на 3-му дні: 1-2 committed, 4-7 втрачені | ✅ commit per-match з `db.rollback()` |
| 38 | `api/main.py:period_to_days` | Невалідний period тихо повертав 30d; клієнт не знав | ✅ `ValueError` → `HTTPException(400)` |
| 39 | `api/main.py:verify_telegram` | `auth_date` не перевірявся → stale токени (> 5хв) приймались | ✅ `abs(now - auth_date) > 300 → None` |
| 40 | `api/main.py` | `STARTING_BALANCE = 1000.0` захардкоджено у 3 місцях | ✅ модульна константа |
| 41 | `HistorySheet.tsx:12` | `formatDate` використовував `getDay()/getDate()` (local) → неправильний день для UTC− | ✅ UTC noon anchor + `getUTCDay/getUTCDate` |
| 42 | `HistorySheet.tsx:47` | `isLoss ? \`-${pick.stake}$\`` → рендерить `-null$` при `stake=null` | ✅ null guard |
| 43 | `api/main.py:164` | `datetime.utcnow()` deprecated Python 3.12+ | ✅ `datetime.now(timezone.utc)` (всі 4 виклики) |
| 44 | `PicksScreen.tsx` | `catch` тільки `setLoading(false)` без error state → порожній екран при збої API | ✅ `picksError` state + `⚠️ Не вдалося завантажити` |

### Ключові висновки pass 2
- `date.today()` в backend = local time сервера, не UTC; на VPS у UTC це безпечно, але явний UTC — правильно
- Module-level `new Date()` у React — небезпечний паттерн для будь-якого state що залежить від часу
- `toISOString()` повертає UTC дату — завжди використовувати `getFullYear/getMonth/getDate` для local date

---

## Related

- [Project Status](project-status.md)
- [Capper Overview](capper-overview.md)

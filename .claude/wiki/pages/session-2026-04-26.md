# Session 2026-04-26 — Infra Refactor + Quick Wins

Велика сесія: відновлення SStats access, 3 Quick Wins, моніторинг, Kelly compounding fix.

## Що зроблено

### Per-league Gem optimization
- Cherry-pick devig (Shin / Proportional) per league — `model/gem/per_league_sweep.py`
- Per-league odds range (1.40-3.50 sweet spot, але різний на лігу)
- `MIN_LO95 = 0.40` guard проти overfitting
- Result: **7 active leagues, 164 picks/24mo, +21.09 u/yr** (raised from +18.61)
- Ligue 1 added; Premier League excluded (lo95=39%)

### Kelly compounding fix
- Backfill ставок раніше використовував статичний bankroll snapshot для всіх picks одного дня → ставки не компаундились
- Створено [scheduler/tasks/rebuild_stakes_chronological.py](../../scheduler/tasks/rebuild_stakes_chronological.py) — chronological replay, idempotent
- Реєстровано як weekly cron (Sun 04:00 UTC) як safety net
- `gem_v1` додано до `KELLY_VERSIONS` у [scheduler/tasks/rebalance_stakes.py](../../scheduler/tasks/rebalance_stakes.py)

### Quick Wins (3 паралельних агенти)

**A. Bookmaker shopping** (`data/best_odds.py`)
- `fetch_odds` тепер віддає всі 8-12 bookmakers, не тільки priority
- Pick decision бере MAX odds для chosen side → +3-5% ROI permanently
- CLV calculation окремо референсує Pinnacle (sharper line)
- 5 unit tests

**E. CLV monitoring + Telegram alerts** (`scheduler/tasks/clv_monitor.py`)
- Daily 08:00 UTC cron → 30d rolling avg CLV per model
- Alert states: healthy / warning / alert (CLV<-2%) + recovery
- State у `model/artifacts/clv_alert_state.json`
- Endpoint `GET /api/clv?model=&days=` для mini-app

**G. Movement filter** (`model/gem/movement_filter.py`)
- Pick filter (Option C) — без retrain Gem
- Skip pick якщо drift > 5%, velocity > 3%, dispersion > 10%
- Toggle: `ENABLE_MOVEMENT_FILTER` у niches.py
- 14 unit tests

### SStats proxy investigation + fix
- **Знахідка:** SStats геоблочить datacenter IPs (DO, Cloudflare datacenter). Тільки residential IPs (Mac) отримують повний 137KB response. Інші — truncated 14762 байт або 95-byte `data:null`.
- Cloudflare Worker `scripts/sstats_proxy_worker.js` deploy не допомагає — теж datacenter
- Mac SSH tunnel впав через orphan ssh на VPS port 8765 (стара сесія тримала port)
- **Fix:** wrapper [`/Users/andrii.zubko/.capper/start_tunnel.sh`](file:///Users/andrii.zubko/.capper/start_tunnel.sh) cleanup orphan ssh на VPS перед `-R` bind
- launchd plist `com.capper.sstats-tunnel` оновлено на wrapper
- Reload успішний: 137KB / 12 bookmakers через `localhost:8765` → socat → container

### Monitoring (`scheduler/tasks/monitor_sstats_proxy.py`)
- Cron кожні 30 хв: hit known fixture через proxy, перевірка response > 50KB
- 3 fails підряд → Telegram alert. Recovery alert при поверненні.
- State у `model/artifacts/sstats_proxy_state.json`

### Housekeeping
- TRACKED_LEAGUES: видалено HNL, Championship, 2.Bundesliga, Liga I (SStats `data:null`)
- 681 backtest predictions з `match_id IS NULL` видалено з `predictions` table
- Telegram alert pipeline активований (`ADMIN_CHAT_ID = 556920093`)

## Critical infrastructure

**Mac SPOF** — інфраструктура ВИМАГАЄ user's Mac up + tunnel:
- launchd: `com.capper.sstats-proxy` (Python proxy на порту 8765)
- launchd: `com.capper.sstats-tunnel` (SSH `-R` зі start_tunnel.sh wrapper)
- На VPS: socat `172.18.0.1:8766 → 127.0.0.1:8765` (systemd `capper-proxy-forwarder.service`)
- Container scheduler/api ходить через `http://172.18.0.1:8766` (per `.env` `SSTATS_API_HOST`)

При відмові Mac → ProxyMon шле Telegram через ~1.5h (3× 30хв).

## Performance snapshot

Models 14d window (з compound Kelly):

| Model | bankroll | settled | WR | ROI per-bet |
|---|---|---|---|---|
| WS Gap | $1732 | 31 | 58% | +17% |
| Aquamarine | $1268 | 7 | 86% | +36% |
| Gem | $1085 | 1 | 100% | +110% |
| Monster | $1060 | 22 | 59% | +4% |
| **Pure** | **$968** | **2** | **50%** | **-19%** |

**Pure underperformance:** Pure not ML — `p_is = wins/n` raw historical hit rate. `compute_pis.py` НЕ використовує OOS_START cutoff (Monster — використовує) → in-sample overfit. Niches refreshed 2026-04-25 на тих самих даних. Recommendation: wait n=20 + fix methodology before next refresh.

## Pending

- League naming refactor merge (з worktree-агента, чекає review)
- API-Football migration $19/mo якщо хочемо позбутись Mac SPOF (зараз вирішено залишити Mac)
- Pure compute_pis OOS_START fix
- 6 leagues які SStats повертає (UPL, Eliteserien, Premiership, Ekstraklasa, Allsvenskan, Süper Lig) — не в Gem TARGET_LEAGUES, потребують retrain з новими league one-hots

## Related

- [Gem Model](gem-model.md)
- [VPS SStats Proxy](vps-sstats-proxy.md)
- [Kelly Criterion](kelly-criterion.md)
- [Closing Line Value](closing-line-value.md)
